import { SkeletonPage } from '@/components/ui/Skeleton';

/** Route-level skeleton shown by Next.js while a dashboard page segment loads. */
export default function DashboardLoading() {
  return <SkeletonPage />;
}
