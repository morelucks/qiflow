{
  "name": "@qiflow/contracts",
  "version": "0.0.1",
  "private": true,
  "description": "QiFlow Smart Contracts for Quai Network",
  "scripts": {
    "build": "hardhat compile",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^4.9.6",
    "ethers": "^6.17.0",
    "hardhat": "^2.29.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "quais": "^1.0.0-alpha.56"
  }
}
